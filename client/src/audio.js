let context;export let muted=localStorage.getItem('escape_muted')==='true';
export function toggle(){muted=!muted;localStorage.setItem('escape_muted',muted);return muted}
export function sound(kind){if(muted)return;try{context ||= new (window.AudioContext||window.webkitAudioContext)();context.resume();const tones=kind==='coin'?[880,1175,1568]:kind==='collision'?[110,145,180]:[620];tones.forEach((f,i)=>{const o=context.createOscillator(),g=context.createGain();o.type=kind==='collision'?'square':'sine';o.frequency.value=f;o.connect(g);g.connect(context.destination);const start=context.currentTime+(kind==='coin'?i*.09:0),gain=kind==='collision'?.16:.14,duration=kind==='coin'?.3:.24;g.gain.setValueAtTime(.0001,start);g.gain.exponentialRampToValueAtTime(gain,start+.015);g.gain.exponentialRampToValueAtTime(.0001,start+duration);o.start(start);o.stop(start+duration+.02)})}catch{}}
